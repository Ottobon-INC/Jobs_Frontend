# ── Frontend (exposed to browser via VITE_ prefix) ──
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_KEY=your-anon-key-here
VITE_API_URL=http://localhost:8001
VITE_MOCK_API_URL=http://localhost:8001/mock
VITE_MOCK_WS_URL=ws://localhost:8001/mock/ws

# ── Server-side only (NOT exposed to browser — no VITE_ prefix) ──
# Used by utility scripts in the project root (check_db*.js, reset.js, etc.)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
